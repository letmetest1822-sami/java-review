package com.sam.enums;

public enum Gender {
    /*
    An enum is a special “class” that represents a group of constants
•enum keyword is used to create an enum(instead of class or
interface), and separate the constants with comma.
•Constants should be in uppercase letters.
•enum can have variables, constructor, method in addition to constants
•Use enums when you have values that you know aren't going to
change, like month days, days, colors, etc.
     */

    MALE, FEMALE;

}
